# Agent 记忆机制 PRD

## 一、竞品调研

| 产品 | 记忆机制 |
|------|----------|
| openclaw | 两层：长期记忆 `MEMORY.md`（决策/上下文/明确记住/教训/观点/错误）+ 每日日记 `memory/YYYY-MM-DD.md`（原始日志）。启动加载长期记忆 + 近3天日记。 |
| hermes | 单层：仅长期记忆，写入用户偏好、要求遵守的规则、纠错、稳定事实、可复用流程、项目约定。每次启动全量加载。 |
| aily | 两层：每日日记（每天对话详情）+ 长期记忆 `MEMORY.md`（明确记住、纠错、有价值信息）。日记分工作日记并做旧日记备份。 |
| workbuddy | 两层：每日工作日志（完成实质工作后追加）+ 长期项目笔记 `MEMORY.md`（长期事实、用户偏好、项目约定）。 |

**总结**：主流方案趋同于"每日日记 + 长期记忆"两层结构；每日层记原始明细、即时追加，长期层记提炼精华（偏好、决策、纠错）；启动时加载长期记忆 + 近几天日记进上下文。

## 二、我们的方案设计

### 1. 设计原则

- 结构极简，只做两层，不过度设计。
- 只记有价值信息，宁缺毋滥。
- 长期记忆是提炼的精华，不是原始日志。

### 2. 记忆结构

```
memory/
├── YYYY-MM-DD.md   ← 每日笔记（原始记录，按天）
└── MEMORY.md       ← 长期记忆（提炼精华，跨会话）
```

- **每日笔记**：按天存储，记录当天对话的具体信息。
- **长期记忆**：单文件，沉淀稳定、可复用的结论与偏好。

### 3. 每日笔记

**写入时机**：产生实质信息后即时追加，不等会话结束。

**写入内容**：
- 对话中产生的决策或结论
- 用户/成员分享的背景、想法、计划
- 提到的日期、截止时间、待办
- 工作相关内容（会议、任务、项目进展）
- 观察到的偏好 / 行为模式

**不写入内容**：
- 纯闲聊，无信息增量
- 过程性吐槽、情绪宣泄
- 已过时且无参考价值的内容
- 用户明确要求不记的内容

### 4. 长期记忆

**核心原则**：
- 只记重要事件、想法、决策、观点、教训。
- 记提炼后的精华，不搬运原始日志。
- 沉淀用户长期偏好。

**写入内容**：

| 类型 | 说明 |
|------|------|
| 决策 | 做出的重要决定 |
| 明确记住 | 用户明确说"记住这个" |
| 教训 | 从错误中学到的 |
| 观点 / 意见 | 形成的看法 |
| 错误记录 | 犯了什么错、原因、如何纠正 |
| 用户长期偏好 | 稳定、可复用的偏好与规则 |

**写入时机**：
- 用户明确说"记住 X"。
- 用户纠正 Agent 的做法。
- 每日笔记中出现值得长期沉淀的信息，提炼后写入。

### 5. 加载机制

每次启动自动加载到上下文：
- 完整 `MEMORY.md`（长期记忆）
- 近 3 天每日笔记（日常上下文）
