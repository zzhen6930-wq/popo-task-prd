# PRD：Agent 技能自沉淀能力（Skill 沉淀）

> 让 Agent 把**跑通的任务流程**自动沉淀成可复用的操作手册。本 PRD 只聚焦一件事：**任务做完后，如何触发、如何把经验沉淀下来。**

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 能力名称 | 技能自沉淀（Skill 沉淀） |
| 归属 | WorkHub Studio · AI 能力 |
| 文档类型 | 能力型 PRD（含产品架构） |
| 范围 | **只写"写 Skill / 沉淀"**；"用 Skill / 检索调用"是已有基础能力，仅作前提说明 |
| 不含 | 模型训练、算法细节 |

---

## 2. 背景与目标

**已有的能力（前提，不是本 PRD 重点）**：每个 Agent 都已能在任务开始前检索技能库、命中 Skill 后加载到上下文、按 Skill 流程执行。**"用"已经通了。**

**缺的能力（本 PRD 要解决）**：技能库里的 Skill 从哪来？现在全靠人工写。Agent 每天跑通大量任务——踩坑修好的流程、被用户纠正后的正确做法、某项目的特殊规则——**做完就丢，不沉淀，技能库不会自己长大。**

**目标**：建一套**沉淀机制**，让 Agent 把有复用价值的成功流程，在任务结束后**自动**提炼、入库、更新。技能库越用越厚，"用"的命中率随之上升。

**一句话价值**：**"用"决定了 Agent 会不会用经验，"写"决定了 Agent 有没有经验可用。本 PRD 补的是后者。**

---

## 3. 最关键的产品判断（沉淀的三条定调）

**① 沉淀不能靠"模型自觉保存"，必须 Runtime 固定触发。**
不能指望模型"突然觉得这次有价值就存一下"——不稳定、会漏、会滥存。正确做法：**每次复杂任务结束，Runtime 无条件触发一次沉淀复盘**，由规则决定这次要不要沉淀、模型只负责生成草稿。**Runtime 驱动，模型辅助。这是整个能力的地基。**

**② 沉淀的不是聊天记录，是提炼后的"操作手册"。**
原样存对话＝存了一堆没法复用的流水账。必须把这次的成功过程**抽象成结构化步骤**：适用场景、前置检查、执行步骤、常见坑、失败处理、验证方式。存的是**方法**，不是**这一次发生的事**。

**③ 沉淀必须有门槛和治理，否则技能库会烂。**
无门槛全存 → 一堆重复、过期、互相冲突的技能（`frontend-build-fix` / `fix-frontend-build` / `vite-debug` 其实是同一个）。所以沉淀不是"生成一个文件"这么简单，而是**判断该不该存 → 判断新建还是更新 → 查重合并 → 质量校验**的完整治理链。

> 结论：写 Skill 的核心不是"让模型写篇 Markdown"，而是 Runtime 里一条 **触发 → 提炼 → 治理 → 入库** 的固定流水线。

---

## 4. 产品架构：沉淀流水线（本 PRD 主体）

任务结束后，Runtime 固定跑一条流水线。每一步都明确"谁触发、谁执行、谁兜底"：

```
任务结束
   │
   ▼
① 触发（Runtime 无条件）  ── 每个复杂任务结束都触发，不依赖模型判断
   │
   ▼
② 采集执行轨迹           ── 全程记的步骤/命令/修正/验证，作为沉淀原料
   │
   ▼
③ 沉淀门槛判定（规则）    ── 这次到底值不值得沉淀？不值得就丢弃，结束
   │  值得
   ▼
④ 结构化提炼（模型）      ── 把成功流程抽象成"操作手册"草稿，非原样存对话
   │
   ▼
⑤ 查重与归并（代码）      ── 新建，还是更新已有 Skill？避免近似技能堆积
   │
   ▼
⑥ 质量校验（规则+模型）   ── 步骤完整、有验证方式、可复用，才准入库
   │
   ▼
⑦ 入库 + 重建索引（代码） ── 存进技能库，更新检索索引，供"用 Skill"命中
```

### 各步骤拆解

**① 触发时机（最关键，决定沉淀会不会发生）**
- **Runtime 固定触发，模型不参与是否触发的决策。**
- 触发条件：任务被判定为"复杂任务"（多步骤 / 有工具调用 / 有报错修正 / 耗时超阈值）即触发。简单问答、闲聊不触发。
- 额外触发点：**用户显式纠正过 Agent 的做法**——这是高价值信号，优先进入沉淀判定。

**② 采集执行轨迹**
- 依赖已有的轨迹记录：这次做了哪些步骤、跑了哪些命令、哪里错了怎么修的、最后怎么验证成功的。
- 这是沉淀的**唯一原料**——没有轨迹就无从提炼。

**③ 沉淀门槛判定（防滥存的第一道闸）**

| 该沉淀（满足即入下一步） | 不该沉淀（命中即丢弃） |
|---|---|
| 流程较复杂（≥5 步） | 一次性聊天 / 简单问答 |
| 中途踩错并修好 | 没有稳定可复用流程 |
| 用户纠正过做法 | 临时事实、一次性数据 |
| 发现了项目/环境的特殊规则 | 任务失败、未跑通 |
| 有明确的验证方式、以后大概率复用 | 中间状态、半成品 |

**④ 结构化提炼（模型的核心活）**
- 把轨迹**抽象成操作手册**，而非复述过程。
- 反例：「今天用户让我修构建，我看了 package.json 跑了些命令就好了」。
- 正例：「pnpm 前端构建失败排查：① 确认 lockfile 是 pnpm-lock.yaml ② 禁用 npm install ③ pnpm install ④ pnpm build ⑤ vite 依赖报错先查 node 版本 ⑥ 以 build 成功为验证」。

**⑤ 查重与归并（防滥存的第二道闸）**
- 提炼出草稿后，先在库里**语义查重**。
- 命中近似技能 → 走**更新/合并**（补充新坑、新步骤），不新建。
- 无近似 → 新建。
- 目标：技能库里一类问题**只有一个权威 Skill**。

**⑥ 质量校验**
- 校验草稿是否达标：**有明确步骤 + 有验证方式 + 场景清晰 + 可复用**。
- 不达标不入库（可丢弃或回炉重提炼）。

**⑦ 入库与重建索引**
- 存入技能库，更新检索索引，让"用 Skill"能在下次相似任务命中。
- 更新已有技能时保留版本，支持回退。

---

## 5. 一个合格 Skill 的产出结构

沉淀产出至少含以下项，缺"步骤"或"验证方式"直接判不合格：

**名称 · 适用场景 · 触发条件 · 前置检查 · 执行步骤 · 常用命令 · 常见坑 · 失败处理 · 验证方式 · 不适用场景**

```markdown
# pnpm 前端构建排查
适用：pnpm 前端项目 install/build/test 失败时
前置：确认是 pnpm 项目（存在 pnpm-lock.yaml）
步骤：① 查 package.json ② 确认 pnpm-lock.yaml ③ 禁用 npm install
      ④ pnpm install ⑤ pnpm build ⑥ 按错误日志分类处理
坑：不要混用 npm/pnpm；node 版本不一致先看 .nvmrc
验证：pnpm build 成功、测试通过、未生成 package-lock.json
不适用：npm/yarn 项目
```

---

## 6. 边界：沉淀进 Skill 还是别处（避免混存）

沉淀触发时，要判断这条经验该进哪，不是什么都塞 Skill：

| 机制 | 存什么 | 一句话 |
|---|---|---|
| **Skill**（本 PRD） | 可复用的流程与方法 | "以后遇到类似任务应该怎么做" |
| **Memory** | 长期偏好、规则、稳定事实 | "我应该长期记住什么"（如：该用户偏好 pnpm） |
| **会话检索** | 历史聊天与任务过程 | "之前具体发生过什么" |

**规则**：沉淀里若是"用户长期偏好/规则"→ 分流进 Memory；"这次具体发生的事"→ 留在会话检索；只有**可复用流程**才进 Skill。

---

## 7. 功能需求（全部围绕"写"）

| 编号 | 需求 | 环节 |
|---|---|---|
| FR-01 | 复杂任务结束**由 Runtime 无条件触发**沉淀复盘，不依赖模型自觉 | 触发 |
| FR-02 | 用户显式纠正过做法的任务，优先进入沉淀判定 | 触发 |
| FR-03 | 全程记录执行轨迹（步骤/命令/修正/验证）作为沉淀原料 | 采集 |
| FR-04 | 按门槛规则判断是否值得沉淀，不达标直接丢弃 | 门槛 |
| FR-05 | 值得沉淀的**提炼为结构化操作手册**，非原样存对话 | 提炼 |
| FR-06 | 提炼后先**语义查重**，命中近似则更新/合并而非新建 | 查重 |
| FR-07 | 质量校验：缺步骤或缺验证方式不予入库 | 校验 |
| FR-08 | 入库并重建检索索引，供"用 Skill"命中 | 入库 |
| FR-09 | 技能库支持去重/合并/更新/废弃/版本/冲突检测 | 治理 |
| FR-10 | 沉淀涉及用户偏好时分流进 Memory，不混存 Skill | 边界 |

---

## 8. MVP 范围

**P0（跑通沉淀最小闭环）**
- Runtime 固定触发 + 沉淀门槛规则
- 执行轨迹采集
- 结构化提炼（模型生成草稿）
- 查重（新建 / 更新）+ 入库 + 重建索引

**P1（治理与增强）**
- 技能治理：合并、废弃、版本、冲突检测
- 质量校验强化（步骤完整性、验证方式必填）
- 用户纠正信号加权触发
- 与 Memory / 会话检索的分流规则细化

**暂不做**：无门槛全自动沉淀、技能自动执行、跨租户技能共享。

---

## 9. 验收标准

1. **触发**：复杂任务结束后 Runtime **必定触发**一次沉淀复盘，简单问答/闲聊不触发。
2. **门槛**：不满足门槛的任务**不产生**任何技能；满足的才进入提炼。
3. **提炼**：产出是**结构化手册**（含步骤 + 验证方式），不是对话流水账。
4. **查重**：相似经验走**更新已有** Skill，不重复新建近似技能。
5. **校验**：缺步骤或缺验证方式的草稿**不入库**。
6. **入库**：沉淀成功后，下次相似任务"用 Skill"能检索命中。
7. **边界**：用户偏好进 Memory、历史过程进会话检索、可复用流程进 Skill，三者不混。

---

## 10. 成功指标

| 指标 | 说明 |
|---|---|
| 沉淀触发率 | 复杂任务结束触发复盘的比例（应≈100%） |
| 沉淀有效率 | 沉淀出的技能中"后续真被复用"的比例（↑，核心指标） |
| 技能库增长 | 由自动沉淀产生的高质量 Skill 数量（↑） |
| 技能库冗余率 | 近似/重复/过期技能占比（↓，衡量查重治理） |
| 重复踩坑率 | 已沉淀 Skill 覆盖的问题再次出错的比例（↓，衡量沉淀价值） |

---

## 11. 一句话总结

**"用 Skill"是每个 Agent 已具备的基础能力；本 PRD 补的是"写 Skill"——一条由 Runtime 固定驱动的沉淀流水线：触发 → 采集 → 门槛 → 提炼 → 查重 → 校验 → 入库。**
模型只负责把成功流程提炼成草稿，**触发时机、沉淀门槛、查重治理全部由 Runtime 兜底**。经验不会自己变成能力，是这条流水线把它变成能力。它与"工具自修正"同源——都是 `Runtime + 存储 + 检索 + 质量控制 + 模型总结` 的自进化范式。
