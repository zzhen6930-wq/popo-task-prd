# PRD：Agent 工具自修正能力（Tool Error Recovery Loop）

> 让 Agent 在工具调用失败时，不停住、不瞎试、不假装成功，而是**看懂错误 → 换做法 → 验证结果 → 沉淀经验**。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 能力名称 | 工具自修正（Tool Error Recovery Loop） |
| 归属 | WorkHub Studio · AI 能力 |
| 文档类型 | 能力型 PRD（含产品架构） |
| 视角 | 产品 + 架构（说清由哪些块构成、如何协同） |
| 不含 | 模型训练、算法细节 |

---

## 2. 背景与目标

**现状**：当前 Agent 工具调用是「成功则继续，失败则报错/停住」。一旦文件路径错、命令不存在、接口 401、测试跑不过，Agent 就卡住或把报错甩给用户，脆弱且不可控。

**目标**：让 Agent 具备一套稳定的错误恢复闭环——

1. 判断这一步**是否真失败**（不把"工具跑了"当成"任务成了"）；
2. 给失败**分类**并说人话；
3. **不原样重复**失败动作，换个有变化的做法重试；
4. 低风险自动修，**高风险问用户**；
5. 完成前**必须验证**；
6. 有复用价值的经验**沉淀**下来。

**一句话价值**：失败不可怕，卡住才可怕，瞎试更可怕，不验证最可怕。

---

## 3. 最关键的产品判断（架构定调）

这个能力**不是写一个更聪明的 Prompt，也不是只做一个 Skill**。

> **Skill 负责"教怎么做"；Runtime 负责"强制必须这么做"；风险层负责"不许乱做"；验证层负责"不许假装做完"；Memory 负责"下次少踩坑"。**

只靠 Skill 会不稳定——Skill 是行为指南，模型可能遵守也可能遗漏。**重试次数、防重复、风险拦截、完成验证这些底线，必须由 Runtime 代码兜底，不能交给模型自觉。**

流程也**不是模型自己决定**"要不要进恢复"，而是 Runtime 写死：**工具失败 → 自动进入恢复闭环**。

---

## 4. 产品架构：五大块能力

整体是一条由 **Orchestrator（编排器）** 固定驱动的闭环。五大块各司其职：

```
用户任务
   │
   ▼
① 感知层  ── 工具结果标准化 · 成败判定
   │  失败
   ▼
② 诊断层  ── 错误分类 · 尝试记录
   │
   ▼
③ 决策层  ── 重试控制 · 恢复策略 · 防重复
   │
   ▼
④ 管控层  ── 风险拦截 · 用户确认  ←（每次执行工具前都过）
   │
   ▼
   执行工具 → 回到 ①
   │  任务就绪
   ▼
⑤ 收尾层  ── 结果验证 · 经验沉淀 · 最终输出
```

### 块一 · 感知层：知道"成没成"

| 小块 | 做什么 | 归属 |
|---|---|---|
| **结果标准化** | 把各种工具的杂乱返回，统一成 `{是否成功, 工具, 输入, 输出, 错误, 错误类型}` 结构 | Runtime，代码 |
| **成败判定** | 不只看有没有报错，还看结果**是否符合预期**（读到空文件、接口 200 但 body 是 error、点击了但页面没变，都算失败） | Runtime，代码 |

**为什么重要**：后面所有判断都依赖这一层给出干净的成败信号。没有它，一切不稳。

### 块二 · 诊断层：知道"为什么没成"

| 小块 | 做什么 | 归属 |
|---|---|---|
| **错误分类** | 把错误归到标准类型（路径错/权限/参数/依赖缺失/认证/限流/超时/测试失败/构建失败/未知）。**规则优先，模型兜底** | Runtime，规则+LLM |
| **尝试记录** | 记录每次调了什么工具、什么输入、什么结果。供"重试计数"和"防重复"使用 | Runtime，代码 |

**分类给用户看的是白话**：不是甩日志，而是"这次不是代码问题，是依赖没装全"。

### 块三 · 决策层：决定"下一步怎么改"

| 小块 | 做什么 | 归属 |
|---|---|---|
| **重试控制** | 按错误类型判断能否自动重试、还剩几次。**纯代码规则**：认证错=不自动重试、超时=最多 3 次、未知=最多 1 次…超预算就停下问用户 | Runtime，配置+代码 |
| **恢复策略库** | 每类错误"可以试哪些动作、禁止哪些动作"。这就是 **Skill/Runbook 的位置**——处理手册，不管流程 | 配置 / Skill |
| **恢复动作生成** | 结合错误类型+策略库，生成"有变化点"的下一步动作。MVP 用规则，增强版让模型在**受限候选内**选 | Runtime，规则/LLM |
| **防重复** | 同一工具+同一输入已失败过、且这次没变化点，就拦截，逼它换策略 | Runtime，代码 |

**核心规则**：每次重试必须有**变化点**（路径变了/参数变了/装了依赖/等待了/换了工具）——没有新信息，不许重来。

### 块四 · 管控层：不许乱做

| 小块 | 做什么 | 归属 |
|---|---|---|
| **风险拦截** | 工具执行**前**判断风险等级（低=只读放行；中=看策略；高=删除/覆盖/上线/付费/不可逆/敏感数据，必须拦） | Runtime，前置中间件 |
| **用户确认** | 高风险动作暂停，用白话说清风险+给方案，等用户拍板 | 对话/权限层 |

**这层必须是系统硬拦截，不能只靠 Skill 里写"删除前要问"**——模型忘了就出事。

### 块五 · 收尾层：不许假装做完 + 会变聪明

| 小块 | 做什么 | 归属 |
|---|---|---|
| **结果验证** | 声称完成前，按任务类型跑验证（改文件→回读确认、修代码→跑测试、调接口→查状态码、操作网页→查页面状态）。**验证不过就回炉** | Runtime，规则+模型辅助 |
| **最终输出** | 输出必带：做了什么 / 是否成功 / **如何验证的** / 没验证则说明原因 | Runtime，输出层 |
| **经验沉淀** | 有复用价值才存。**事实/偏好→Memory**（"该项目用 pnpm"）；**流程→Skill**（"该项目构建失败的排查步骤"）；临时问题不存 | Memory / Skill |

---

## 5. 各块协同：一次完整闭环

以「读取并修改 config.yaml，但路径错了」为例：

1. **感知**：`read_file("./config.yaml")` → 标准化为 `失败, file_not_found`。
2. **诊断**：分类=路径错误；记录本次尝试。
3. **决策**：重试控制判定"路径错可自动修，剩 2 次"→ 策略库给出"搜索相似文件"→ 生成动作 `search_files("*config*")`（相比上次**有变化点**，防重复放行）。
4. **管控**：搜索是只读=低风险，放行。
5. 执行 → 找到 `./backend/config.yaml` → 用新路径重读 → **感知**判定成功。
6. **收尾**：修改后回读文件确认改动；验证通过；输出"原路径没找到，我改为搜索并定位到 backend/config.yaml，已修改并回读确认"。

**对比认证失败（401）**：诊断=认证错 → 重试控制判定**不自动重试** → 直接进管控层问用户"请提供有效凭证"，**不重试、不编 Token、不假装成功**。

---

## 6. Skill 到底是什么定位（避免误解）

> **Skill = 恢复策略库 / 排查手册，只回答"某类错误怎么排查"，不负责整个闭环、不负责强制约束。**

首批可做 5 个 Runbook 型 Skill：文件错误、命令错误、依赖错误、API 错误、测试失败。

举例 `file-error-recovery`：`检查当前目录 → 搜索相似文件 → 唯一候选改用新路径 → 多候选问用户 → 禁止重复读同一路径`。

但**重试次数、防重复、风险拦截、验证**这些**不放 Skill**，放 Runtime 强制。

---

## 7. 功能需求（关键项）

| 编号 | 需求 | 归属块 |
|---|---|---|
| FR-01 | 每步工具调用产出标准化结果，能判定真实成败 | 感知 |
| FR-02 | 工具"执行了"不得等同任务"成功了" | 感知 |
| FR-03 | 失败必须分类，并给用户白话原因 | 诊断 |
| FR-04 | 记录每次尝试（工具+输入+结果） | 诊断 |
| FR-05 | 同一失败动作无变化点时禁止重复 | 决策 |
| FR-06 | 每类错误设最大自动重试次数，超限即停并说明卡点 | 决策 |
| FR-07 | 每次重试必须含变化点 | 决策 |
| FR-08 | 高风险动作执行前拦截并要求用户确认 | 管控 |
| FR-09 | 认证类错误默认不自动重试，转用户 | 管控 |
| FR-10 | 声称完成前必须验证，验证不过回炉 | 收尾 |
| FR-11 | 最终输出必带验证依据；无法验证需说明 | 收尾 |
| FR-12 | 仅沉淀有复用价值的经验，偏好入 Memory、流程入 Skill | 收尾 |

---

## 8. 风险分级与拦截清单

| 等级 | 含义 | 处理 |
|---|---|---|
| 低 | 只读、搜索、检查、局部验证 | 自动执行 |
| 中 | 改本地文件/环境、装依赖 | 按产品策略，默认可自动+可回退 |
| 高 | 删除、覆盖、上线部署、大版本升级、付费、不可逆、敏感数据 | **必须用户确认** |

**必拦关键词/动作**：`delete / overwrite / deploy / force push / drop / volume 清空 / 付费调用 / 读取或发送密钥·隐私`。

---

## 9. MVP 范围

**P0（必做，构成最小闭环）**
- 工具结果标准化 + 成败判定
- 错误分类（先支持 10 类，规则实现）
- 重试控制（配置化：每类错误 auto/max_retries）
- 尝试记录 + 防重复
- 风险拦截（高风险清单）
- 完成验证（输出必带验证状态）

**P1（增强）**
- 5 个错误 Runbook Skill
- 项目级 Memory（固定命令/包管理器/禁止操作）
- 恢复动作生成接入 LLM（受限候选内选）
- 失败复盘报告

**暂不做**：所有错误精准归因、全自动修复一切、可视化调试面板、跨任务经验图谱。

---

## 10. 验收标准

**恢复行为**：工具失败时，Agent 必须——明确知道失败、不假装成功、不无意义重复、给白话原因、提出有变化的下一步、低风险自动推进、高风险问用户、超重试预算即停并说明卡点。

**结果验证**：声称完成时，必须给出至少一种依据（测试通过 / 文件已更新回读 / 接口成功 / 页面状态变化 / 服务已起 / 产物已生成）。

**风险管控**：删除、覆盖、上线、大版本升级、付费、不可逆、敏感数据——执行前**均须用户确认**。

---

## 11. 成功指标

| 指标 | 说明 |
|---|---|
| 失败后继续推进率 | 工具失败后 Agent 能自行推进的比例（↑） |
| 重复失败率 | 同一错误被原样重复的次数（↓） |
| 用户接管率 | 用户被迫手动指导排错的比例（↓） |
| 高风险拦截率 | 高风险操作执行前被确认的比例（应≈100%） |
| 已验证完成率 | 声称完成且带验证依据的比例（↑） |
| 任务完成率 | 最终成功完成的比例（↑） |

---

## 12. 一句话总结

**这是 Runtime 层的一套错误恢复闭环，不是一个 Skill。**
Runtime 固定驱动「标准化 → 分类 → 重试判断 → 生成恢复动作 → 防重复 → 风险拦截 → 再执行 → 验证 → 沉淀」；Skill 是其中的排查手册，Memory 是长期经验，Prompt 统一行为风格。核心底线（防重复、限重试、拦风险、强制验证）由代码兜底，不交给模型自觉。
